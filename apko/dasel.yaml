contents:
  repositories:
    - ./packages
    - https://packages.wolfi.dev/os

  keyring:
    - ./melange.rsa.pub
    - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub

  packages:
    - wolfi-baselayout
    - dasel=3.3.1-r1

entrypoint:
  command: /usr/bin/dasel

archs:
  - amd64